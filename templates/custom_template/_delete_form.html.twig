<form method="post" action="{{ path('custom_template_delete', {'id': custom_template.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ custom_template.id) }}">
    <button class="btn">Delete</button>
</form>
